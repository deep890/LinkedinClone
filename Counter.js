import React from 'react'

function Counter() {
    return (
        <div className = "counter">
            <div className = {}>
                <button className = {} onClick = {() => {dispatch(increament())}}>+</button>
            </div>
        </div>
    )
}

export default Counter
